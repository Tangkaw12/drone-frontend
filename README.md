# 🌐 Drone Frontend (Assignment #2)

เว็บ Frontend แบบ **Static** สำหรับใช้งานร่วมกับ Drone API — มี 3 หน้า:
**View Config • Temperature Log Form • View Logs**  
This is a **3-page static web app** that consumes your own `drone-api`.

---

## 👤 ผู้จัดทำ (Author)
- **ชื่อ:** นางสาวรุ่งอรุณ แก้วสุวรรณ์
- **รหัสนักศึกษา:** 66010710
- **Drone Name:** Iron Wolf (Russia)
- **ภาควิชา:** ไอโอทีและฟิสิกส์อุตสาหกรรม

---

## 💻 วิธีใช้งาน (How to Run Locally)

### 1) โคลนโปรเจกต์
```bash
git clone <YOUR_REPO_URL>
cd drone-frontend
```

### 2) ตั้งค่า `config.js`
แก้ไฟล์ `config.js` ให้ชี้ API ของคุณ:
```js
window.APP_CONFIG = {
  API_BASE: "http://localhost:3000",  // เปลี่ยนเป็น URL ของ API เมื่อ deploy แล้ว
  DRONE_ID: "66010710"
};
```

### 3) เปิดเว็บ
วิธีง่ายสุด (ไม่ต้องติดตั้งอะไรเพิ่ม): ดับเบิลคลิกเปิด `index.html`  
หรือเสิร์ฟผ่าน dev server เพื่อให้ CORS/Path เสถียรกว่า:
```bash
npx http-server . -p 5173
# แล้วเข้า http://localhost:5173
```

---

## 🧪 ฟีเจอร์ที่มี (Features)
- **View Config**: เรียก `GET /configs/:DRONE_ID` แล้วแสดงผล
- **Temperature Log Form**: กรอก `celsius` แล้วส่ง `POST /logs` พร้อมคีย์ `drone_id`, `drone_name`, `country` จาก config
- **View Logs**: เรียก `GET /logs/:DRONE_ID` แสดงล่าสุดก่อน จำกัด 12 พร้อมปุ่ม Next/Prev

---

## 🧾 โครงสร้างโปรเจกต์ (Project Structure)
```
drone-frontend/
├─ index.html
├─ styles.css
├─ config.js
└─ main.js
```

---

## ☁️ Deployment

### ทางเลือก A) Netlify (แนะนำสำหรับ static)
1. สร้าง repo บน GitHub แล้ว push โค้ดขึ้นไป
2. ไปที่ Netlify → **Add new site** → **Import from Git** → เลือก repo `drone-frontend`
3. Build settings (เพราะเป็น static)  
   - **Build command:** _(leave empty)_  
   - **Publish directory:** `/`
4. Deploy เสร็จจะได้ URL เช่น `https://drone-frontend.netlify.app`
5. เปิดไฟล์ `config.js` ใน repo แล้วแก้ให้
```js
window.APP_CONFIG.API_BASE = "https://<your-api-on-render>.onrender.com";
```
6. Commit & Deploy (Netlify จะอัปเดตอัตโนมัติ)

### ทางเลือก B) Vercel
1. **New Project** → Import GitHub repo `drone-frontend`
2. Framework Preset: **Other**
3. Output Directory: `/`
4. Deploy แล้วแก้ `config.js` ให้ `API_BASE` ชี้ไปยัง URL ของ API (เช่นจาก Render)
```js
window.APP_CONFIG.API_BASE = "https://drone-api.onrender.com";
```

> หมายเหตุ: หากผู้ใช้เปิดเว็บจากเครือข่ายอื่น ให้ใช้โดเมนสาธารณะของ API (ไม่ใช่ `localhost`).

---

## 🔍 Quick Test Checklist
- เปิดเว็บ → กด **View Config** → เห็นข้อมูล Iron Wolf
- กรอก **Temperature Log Form** → กด Submit → แจ้ง “ส่งข้อมูลสำเร็จ ✓”
- กด **View Logs** → เห็นรายการใหม่อยู่บนสุด (เรียงล่าสุดก่อน)

---

## ✅ Status
| Page | Status | Description |
|------|:-----:|-------------|
| View Config | 🟢 | ดึงข้อมูลจาก API ได้ |
| Temperature Log Form | 🟢 | POST log สำเร็จ |
| View Logs | 🟢 | แสดงล่าสุดก่อน จำกัด 12 |

© 2025 • Drone Frontend Project